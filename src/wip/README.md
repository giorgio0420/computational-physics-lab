# Not working

These two programs are published as they were written. They do not produce correct
output and they are excluded from the default `make` target. The specific defects are
listed in the repository README, under "Not working".

Repairing them would mean rewriting them rather than fixing them, so they are left
here as a record instead of being silently corrected or deleted.
